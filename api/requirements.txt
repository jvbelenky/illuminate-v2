# Core API framework
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
pydantic>=2.0.0,<3.0.0
python-multipart>=0.0.6,<1.0.0

# UV calculations library (brings matplotlib, scipy, numpy as transitive deps)
# Pin to specific version for security and reproducibility
guv_calcs==0.6.2

# Explicit transitive dependencies for clarity
# (these are required by guv_calcs but listed here to document usage)
# matplotlib - used for plot generation
# scipy - used for Delaunay triangulation in photometric web
# numpy - used throughout for array operations
